version: "3.8"
services:
  backend:
    build: ./course-service
    container_name: course-service
    ports:
      - "8080:8080"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "3000:80"
    restart: unless-stopped
